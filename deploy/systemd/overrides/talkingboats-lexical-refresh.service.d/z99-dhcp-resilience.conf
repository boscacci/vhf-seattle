[Service]
# Clear the old fixed-DHCP readiness drop-in before applying the subnet-aware
# readiness probe.  This file intentionally sorts after legacy local drop-ins.
ExecStartPre=
ExecStartPre=/bin/bash %h/repos/elliott-bay-vhf/.runtime/live-ais-deploy/scripts/talkingboats_lan_address.sh --dns-host dynamodb.us-west-2.amazonaws.com
